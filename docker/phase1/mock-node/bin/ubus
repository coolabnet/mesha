#!/usr/bin/env bash
set -euo pipefail

if [[ "${1:-}" != "call" ]]; then
  exit 1
fi

case "${2:-}:${3:-}" in
network.interface:dump)
  cat /mock-current/interface-dump.json
  ;;
network.wireless:status)
  cat /mock-current/wireless-status.json
  ;;
*)
  exit 1
  ;;
esac
